# Screenshots

Generated: 2026-07-31T22:37:06-03:00

Screenshots must be added for UI changes and every publication when possible.

## Files

- None detected


## Missing Screenshots

Nenhuma screenshot ainda. A UI web (`apps/web`) foi validada por requisições
HTTP reais (curl) contra o dashboard renderizado, não por captura visual — não
há navegador com display disponível neste ambiente para capturar imagem. O
app iOS não foi compilado nesta sessão (sem Xcode/macOS disponível), então
também não há screenshot possível ainda. Próximo passo: capturar telas de
login, visão geral e configurações em Web (via navegador real) e iOS (via
simulador, depois que o build for validado no Xcode/CI) antes da próxima
atualização deste handoff.
