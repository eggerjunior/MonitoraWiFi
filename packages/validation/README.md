# packages/validation — Regras de validação compartilhadas

Validação de hosts, portas, URLs, CIDRs e parâmetros de ferramentas de rede
(anti-SSRF, anti-command-injection). Ver `docs/security/threat-model.md`.
